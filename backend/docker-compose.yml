version: '3.8'

services:
  backend:
    build: .
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - DATABASE_URL=file:/app/dev.db
    env_file:
      - .env
    volumes:
      - ./dev.db:/app/dev.db
    restart: always
    network_mode: "host"
